这个工程主要围绕触觉方向预测数据集、训练脚本、评估脚本和结果对比流程展开。它支持不同数据模式的训练与测试，也能把不同模型在同一组测试角度上的表现统一拿来比较，方便观察模型是否真的学到了对旋转不敏感、但对方向敏感的规律。compare 脚本就是把 baseline 和 equivariant 两类模型在 mode1 和 mode2 下的结果放到同一张表里，并画出误差随旋转角度变化的曲线，便于直接看出哪种模型更稳、哪种模型在某些角度下更容易出错。

